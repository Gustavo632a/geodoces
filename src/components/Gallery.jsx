import { useState } from 'react';
import { gallery } from '../data/business';
import { messages, whatsappUrl } from '../utils/whatsapp';
export default function Gallery(){ const [selected,setSelected]=useState(null); if(!gallery.length)return null; return <section id="galeria" className="section gallery-section"><div className="section-heading centered"><p className="eyebrow">INSPIRAÇÃO EM CADA DETALHE</p><h2>Difícil é escolher só um.</h2><p>Imagens ilustrativas enquanto preparamos a galeria da Geo Doces.</p></div><div className="gallery">{gallery.map((item,i)=><button key={item.image} className={`gallery__item gallery__item--${i+1}`} onClick={()=>setSelected(item)}><img src={item.image} alt={item.alt} loading="lazy"/></button>)}</div><div className="center-action"><a className="button" href={whatsappUrl(messages.order)} target="_blank" rel="noreferrer">Quero fazer meu pedido <span>↗</span></a></div>{selected&&<div className="lightbox" role="dialog" aria-modal="true" aria-label="Imagem ampliada" onClick={()=>setSelected(null)}><button aria-label="Fechar imagem">×</button><img src={selected.image} alt={selected.alt}/></div>}</section> }
