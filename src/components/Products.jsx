import { categories, products } from '../data/business';
import { messages, whatsappUrl } from '../utils/whatsapp';
function Item({ product }) { return <article className="product"><div className="product__image">{product.image ? <img src={product.image} alt={product.name} loading="lazy"/> : <div className="image-empty">Foto do produto</div>}</div><div className="product__body"><h3>{product.name}</h3>{product.description && <p>{product.description}</p>}{product.price && <strong>{product.price}</strong>}{product.available === false ? <em>Indisponível no momento</em> : <a href={whatsappUrl(messages.product(product.name))} target="_blank" rel="noreferrer">Quero esse <span>→</span></a>}</div></article> }
export default function Products() {
 const featured = products.filter(p => p.featured && p.available !== false);
 return <><section id="cardapio" className="section products-section"><div className="section-heading"><p className="eyebrow">SELECIONADOS COM CARINHO</p><h2>Nossos queridinhos.</h2><p>Uma seleção especial para deixar seu momento mais doce.</p></div>{featured.length ? <div className="products-grid">{featured.map((p, i) => <Item key={i} product={p}/>)}</div> : <div className="empty-state"><span>✦</span><p>O cardápio da Geo está sendo preparado com carinho.</p><a className="text-link" href={whatsappUrl(messages.general)} target="_blank" rel="noreferrer">Consultar opções disponíveis <span>↗</span></a></div>}</section>
 <section className="section menu"><div className="section-heading"><p className="eyebrow">CARDÁPIO</p><h2>Escolha seu favorito.</h2></div>{categories.length > 0 && <div className="chips">{categories.map(c => <button key={c}>{c}</button>)}</div>}{products.length > 0 && <div className="products-grid">{products.map((p,i)=><Item key={i} product={p}/>)}</div>}</section></>;
}
